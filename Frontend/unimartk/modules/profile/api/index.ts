import axios from "@/lib/axios";
import { Product } from "@/modules/products/types";
import { Skill } from "@/modules/skills/types";

// export const fetchproductCategories = async (): Promise<Product[]> => {
//   // add logic to ckeck if user is actually superadmin
//   const response = await axios.get(`/categories/`);
//   return response.data;
// };
// export const fetchProductSubcategories = async (): Promise<Skill[]> => {
//   // add logic to ckeck if user is actually superadmin
//   const response = await axios.get(`/subcategories/`);
//   return response.data;
// };
